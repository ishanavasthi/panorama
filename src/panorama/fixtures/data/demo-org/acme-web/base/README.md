# acme-web

Browser client for the Acme link service. It fetches links from `acme-api` and
renders them. Shared helpers (URL validation, formatting) come from
`acme-shared` rather than being reimplemented here.
